# Для передачи другой нейронке / разработчику

## Краткая суть проекта

**AI-Booking** - SaaS платформа для автоматизации записи клиентов.

**Для кого:** Индивидуальные мастера (парикмахеры, массажисты, косметологи)

**Главная фича:** AI-секретарь в Telegram, который понимает естественный язык:
- "Запиши меня на пятницу после обеда" → AI находит слоты и создает запись
- "Перенеси мою запись на час позже" → AI переносит
- "Сколько стоит стрижка?" → AI отвечает из базы

---

## Архитектура (высокоуровнево)

```
┌─────────────┐
│   КЛИЕНТ    │
└──────┬──────┘
       │
       ├─► Web Widget (/{slug}) - Публичная страница записи
       │
       ├─► Telegram Bot - AI-диалог для записи
       │
       └─► Instagram DM (planned)
              │
              ▼
       ┌──────────────┐
       │   BACKEND    │
       │  (Supabase)  │
       └──────┬───────┘
              │
       ┌──────┴───────┐
       │              │
       ▼              ▼
┌──────────┐    ┌──────────┐
│PostgreSQL│◄───┤  OpenAI  │
│   + RLS  │    │  GPT-4o  │
└──────────┘    └──────────┘
       ▲
       │
       ▼
┌──────────────┐
│   МАСТЕР     │
│  Dashboard   │
└──────────────┘
```

---

## Что уже работает (100% готово)

### 1. **Vendor Dashboard** - панель для мастеров
- Регистрация/вход
- Управление услугами (добавить, редактировать, удалить)
- Календарь всех записей
- База клиентов
- Настройки профиля и рабочего графика
- QR-код для печати/распространения
- Ручное создание записей

**Маршруты:**
- `/dashboard` - главная
- `/dashboard/services` - услуги
- `/dashboard/calendar` - календарь
- `/dashboard/clients` - клиенты
- `/dashboard/settings` - настройки
- `/dashboard/qr-code` - QR-код
- `/dashboard/bookings/new` - новая запись

### 2. **Публичная страница записи**
- `/{slug}` - уникальная страница каждого мастера
- Клиент выбирает услугу → дата → время
- Создается запись в БД
- Отправляется email подтверждение

### 3. **AI Price Recognition**
- Мастер загружает фото прайс-листа
- GPT-4 Vision распознает услуги, цены, длительность
- Возвращает структурированный JSON
- Можно автоматически создать услуги

**API:** `POST /api/recognize-price-list`

---

## Что в процессе разработки

### **Sprint 3: Telegram Bot** (0% готовности)

**Цель:** Клиент пишет боту в Telegram → AI обрабатывает → создает запись

**Компоненты:**
1. `supabase/functions/telegram-webhook/` - обработчик вебхуков
2. AI Orchestrator - логика диалога с GPT-4
3. Function Calling - инструменты для AI:
   - `get_services` - получить список услуг
   - `check_availability` - проверить свободные слоты
   - `create_booking` - создать запись
   - `cancel_booking` - отменить
   - `reschedule_booking` - перенести

**Промпт для AI:** `/prompts/01-AI-BOOKING-AGENT.md`

---

## База данных: ключевые моменты

### Главные таблицы

**vendors** - Мастера
- `id`, `user_id`, `slug`, `business_name`
- `timezone`, `currency`, `locale`
- `ai_config` (настройки AI)
- `plan` (start/growth/pro)

**services** - Услуги
- `vendor_id`, `name`, `price`, `duration_minutes`
- `category`, `is_active`, `is_online_bookable`
- `embedding` (vector для RAG)

**bookings** - Записи
- `vendor_id`, `client_id`, `service_id`
- `start_time`, `end_time`
- `status` (pending/confirmed/cancelled)
- `source` (web/telegram/instagram/manual)

**work_schedules** - График работы
- `vendor_id`, `day_of_week` (0-6)
- `start_time`, `end_time`
- `is_working_day`, `breaks` (json)

**conversations** + **messages** - Диалоги с AI
- История чатов с ботом
- Контекст (выбранная услуга, намерение)
- Токены использованные

### Важно!

**RLS включен везде!** Каждый vendor видит только свои данные.
```sql
CREATE POLICY "Vendors can view own data" 
ON services FOR SELECT 
USING (vendor_id IN (SELECT id FROM vendors WHERE user_id = auth.uid()));
```

---

## Переменные окружения (критичные)

```env
# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://jcczperyfdjwvcjiqrvj.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<ключ>
SUPABASE_SERVICE_ROLE_KEY=<ключ>  # Для Admin API

# OpenAI
OPENAI_API_KEY=sk-...  # Для AI бота и распознавания

# Telegram
TELEGRAM_BOT_TOKEN=123456789:ABC...
TELEGRAM_WEBHOOK_SECRET=random_string

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000  # В проде - Vercel URL
```

**Получить Supabase ключи:**
https://supabase.com/dashboard/project/jcczperyfdjwvcjiqrvj/settings/api

---

## Как запустить (для новой нейронки)

```bash
# 1. Клонировать
git clone https://github.com/NurchikLuchshe/Hustle.git
cd Hustle

# 2. Установить зависимости
npm install

# 3. Создать .env.local
cp .env.example .env.local
# Заполнить все ключи (см. выше)

# 4. База данных
# Вариант A: Через CLI
supabase db push

# Вариант B: Вручную
# Скопировать docs/02-DATABASE-SCHEMA.sql
# Вставить в Supabase SQL Editor
# https://supabase.com/dashboard/project/jcczperyfdjwvcjiqrvj/sql/new

# 5. Запустить
npm run dev
```

Откроется http://localhost:3000

---

## Типичные проблемы

### "Invalid login credentials"
→ Пользователь не существует. Зарегистрируйтесь на `/register`

### База данных пустая
→ Применить SQL схему из `docs/02-DATABASE-SCHEMA.sql`

### TypeScript ошибки типов БД
```bash
npm run db:types  # Регенерирует src/shared/types/database.types.ts
```

### Dev сервер не стартует
```bash
rm -rf .next node_modules
npm install
npm run dev
```

---

## Следующие шаги разработки

1. **Telegram Bot** (Priority 1)
   - Вебхук handler
   - AI Orchestrator
   - Function calling
   - Тестирование диалогов

2. **Instagram DM** (Priority 2)
   - Facebook Graph API
   - Webhook handler

3. **Платежи** (Priority 3)
   - Stripe/YooKassa
   - Подписки PRO

4. **Аналитика** (Priority 4)
   - Dashboard метрики
   - Конверсия записей

---

## Полезные файлы для изучения

1. `docs/01-TECHNICAL-SPECIFICATION.md` - полное ТЗ
2. `docs/02-DATABASE-SCHEMA.sql` - схема БД
3. `docs/03-PROJECT-ROADMAP.md` - детальный roadmap
4. `prompts/01-AI-BOOKING-AGENT.md` - system prompt для бота
5. `prompts/03-DEVELOPMENT-PROMPTS.md` - промпты для разработки

---

## Контакты авторов

- **GitHub:** NurchikLuchshe/Hustle
- **Supabase Project ID:** jcczperyfdjwvcjiqrvj

---

**Удачи в дебаге! 🚀**
