# 🚀 Быстрый старт: Настройка Supabase

Этот гайд поможет создать и настроить Supabase проект для AI-Booking.

## Шаг 1: Создание Supabase проекта

1. **Перейдите на** https://supabase.com
2. **Нажмите** "Start your project" или "New Project"
3. **Залогиньтесь** через GitHub
4. **Создайте организацию** (если её нет)
5. **Создайте новый проект:**
   - **Project name:** `ai-booking` (или любое другое)
   - **Database Password:** Придумайте надёжный пароль (сохраните!)
   - **Region:** Выберите ближайший регион (Europe - Frankfurt рекомендуется)
   - **Pricing Plan:** Free (для начала)
6. **Дождитесь** создания проекта (~2 минуты)

## Шаг 2: Получение API ключей

После создания проекта:

1. **Перейдите** в Settings → API
2. **Скопируйте:**
   - `Project URL` → это ваш `NEXT_PUBLIC_SUPABASE_URL`
   - `anon public` ключ → это `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - `service_role secret` ключ → это `SUPABASE_SERVICE_ROLE_KEY` ⚠️ Секретный!

## Шаг 3: Настройка переменных окружения

1. **Откройте файл** `.env.local` в корне проекта
2. **Вставьте** полученные ключи:
   ```env
   NEXT_PUBLIC_SUPABASE_URL=https://ваш-проект.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш-anon-ключ
   SUPABASE_SERVICE_ROLE_KEY=ваш-service-role-ключ
   ```

## Шаг 4: Применение миграций базы данных

### Вариант А: Через Supabase Dashboard (Рекомендуется)

1. **Откройте** Supabase Dashboard → SQL Editor
2. **Создайте** New Query
3. **Скопируйте** весь код из файла `supabase/migrations/20260116_initial_schema.sql`
4. **Вставьте** в SQL Editor
5. **Нажмите** Run (или F5)
6. **Проверьте** что выполнилось без ошибок

### Вариант Б: Через Supabase CLI

```bash
# Установите Supabase CLI (если нет)
npm install -g supabase

# Залогиньтесь
npx supabase login

# Свяжите проект
npx supabase link --project-ref ваш-project-id

# Примените миграции
npx supabase db push
```

## Шаг 5: Проверка

1. **Откройте** Supabase Dashboard → Table Editor
2. **Убедитесь** что созданы таблицы:
   - ✅ vendors
   - ✅ services
   - ✅ work_schedules
   - ✅ clients
   - ✅ bookings
   - ✅ conversations
   - ✅ messages
   - ✅ embeddings
   - ✅ verification_codes
   - ✅ qr_links
   - ✅ schedule_exceptions

3. **Проверьте расширения:** Database → Extensions
   - ✅ uuid-ossp (enabled)
   - ✅ vector (enabled для pgvector)

## Шаг 6: Запуск проекта

```bash
# Установите зависимости (если не установлены)
npm install

# Запустите dev-сервер
npm run dev
```

Откройте http://localhost:3000 - не должно быть ошибок подключения!

## 🎉 Готово!

Теперь у вас настроен:
- ✅ Supabase проект с базой данных
- ✅ 11 таблиц с RLS политиками
- ✅ Векторный поиск (pgvector)
- ✅ Функция получения свободных слотов

**Следующие шаги:**
- Создание страниц авторизации
- Dashboard для мастеров
- Telegram Bot интеграция

---

## ❓ Проблемы?

### Ошибка: "Extensions not found"
Перейдите в Database → Extensions и включите:
- `uuid-ossp`
- `vector`

### Ошибка: "RLS policies failed"
Убедитесь что таблицы созданы до применения политик.

### Ошибка подключения в Next.js
Проверьте:
1. Правильность ключей в `.env.local`
2. Перезапустите dev сервер (`npm run dev`)
